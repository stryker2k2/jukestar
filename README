# JukeStar
Star Ranch's Custom Poolside JukeBox

### Dependencies
- (apt) python3
- (pip) flask

### Setup
- apt install python3.10-venv
- python3 -m venv virt
- source virt/bin/activate
- pip install (dependencies)
- pip freeze
- flask run --debug